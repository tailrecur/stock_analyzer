<%= form_for(@quarterly_result) do |f| %>
  <% if @quarterly_result.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quarterly_result.errors.count, "error") %> prohibited this quarterly_result from being saved:</h2>

      <ul>
      <% @quarterly_result.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
