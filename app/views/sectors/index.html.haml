%h1 Listing sectors

%table
  %tr
    %th Name
    %th Debt Ratio
    %th
    %th
    %th

  - @sectors.each do |sector|
    %tr
      %td= sector.name
      %td.price= number_with_precision(sector.debt_ratio, :precision => 2)
      %td= link_to 'Show', sector
      %td= link_to 'Edit', edit_sector_path(sector)
      %td= link_to 'Destroy', sector, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Sector', new_sector_path
